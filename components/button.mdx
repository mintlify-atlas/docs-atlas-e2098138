---
title: Button
description: Interactive button component for triggering actions and navigation
---

The Button component provides a versatile way to trigger actions or navigate within your application. It supports multiple variants, colors, and states including loading and disabled states.

## Basic Usage

```jsx
import { Button } from 'reshaped';

function App() {
  return (
    <Button variant="solid" color="primary">
      Click me
    </Button>
  );
}
```

## Variants

Button supports four visual variants:

```jsx
<Button variant="solid">Solid</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="faded">Faded</Button>
```

## Colors

Button can be displayed in multiple colors:

```jsx
<Button color="primary">Primary</Button>
<Button color="critical">Critical</Button>
<Button color="positive">Positive</Button>
<Button color="neutral">Neutral</Button>
<Button color="media">Media</Button>
```

## Sizes

Button supports four size options:

```jsx
<Button size="small">Small</Button>
<Button size="medium">Medium</Button>
<Button size="large">Large</Button>
<Button size="xlarge">Extra Large</Button>
```

## With Icons

Add icons to the start or end position:

```jsx
import { Button } from 'reshaped';
import IconPlus from '@/icons/Plus';
import IconArrowRight from '@/icons/ArrowRight';

<Button icon={IconPlus}>Add Item</Button>
<Button endIcon={IconArrowRight}>Continue</Button>
<Button icon={IconPlus} /> {/* Icon only */}
```

## States

### Loading State

Show a loading indicator while an action is in progress:

```jsx
<Button loading loadingAriaLabel="Loading...">
  Submit
</Button>
```

### Disabled State

Disable user interaction:

```jsx
<Button disabled>Disabled</Button>
```

### Highlighted State

Highlight the button when it represents an active state:

```jsx
<Button highlighted>Active</Button>
```

## Navigation

Use Button as a link by providing an href:

```jsx
<Button href="/dashboard">Go to Dashboard</Button>
```

## Styling Options

### Rounded

Create a circular button:

```jsx
import IconPlus from '@/icons/Plus';

<Button rounded icon={IconPlus} />
```

### Elevated

Apply elevation shadow:

```jsx
<Button elevated>Elevated Button</Button>
```

### Full Width

Make the button take the full width of its container:

```jsx
<Button fullWidth>Full Width Button</Button>
```

## Button Group

Group multiple buttons together:

```jsx
import { Button } from 'reshaped';

function App() {
  return (
    <Button.Group>
      <Button>First</Button>
      <Button>Second</Button>
      <Button>Third</Button>
    </Button.Group>
  );
}
```

## Button Aligner

Align a button to a specific side within a container:

```jsx
import { Button } from 'reshaped';

function App() {
  return (
    <Button.Aligner side="end">
      <Button>Aligned Button</Button>
    </Button.Aligner>
  );
}
```

## Props

### Button

<ParamField path="variant" type="'solid' | 'outline' | 'ghost' | 'faded'" default="solid">
  Component render variant
</ParamField>

<ParamField path="color" type="'primary' | 'critical' | 'positive' | 'neutral' | 'media' | 'inherit'" default="neutral">
  Component color scheme
</ParamField>

<ParamField path="size" type="'small' | 'medium' | 'large' | 'xlarge'" default="medium">
  Component size (responsive)
</ParamField>

<ParamField path="icon" type="React.ComponentType">
  SVG component for the icon at the start position
</ParamField>

<ParamField path="endIcon" type="React.ComponentType">
  SVG component for the icon at the end position
</ParamField>

<ParamField path="loading" type="boolean" default="false">
  Show loading state
</ParamField>

<ParamField path="loadingAriaLabel" type="string">
  aria-label attribute for the loading indicator
</ParamField>

<ParamField path="disabled" type="boolean" default="false">
  Disable user interaction
</ParamField>

<ParamField path="rounded" type="boolean" default="false">
  Change border radius to fully rounded corners
</ParamField>

<ParamField path="elevated" type="boolean" default="false">
  Apply elevated styles to the component
</ParamField>

<ParamField path="fullWidth" type="boolean" default="false">
  Make the component take the full width of the parent element (responsive)
</ParamField>

<ParamField path="highlighted" type="boolean" default="false">
  Highlight the component when used for an active state
</ParamField>

<ParamField path="href" type="string">
  URL to navigate to when clicked
</ParamField>

<ParamField path="onClick" type="(event: React.MouseEvent) => void">
  Callback when the button is clicked
</ParamField>

<ParamField path="type" type="'button' | 'submit' | 'reset'">
  HTML button type attribute
</ParamField>

<ParamField path="stopPropagation" type="boolean">
  Stop event propagation when clicked
</ParamField>

<ParamField path="as" type="React.ElementType">
  Override the root element type
</ParamField>

<ParamField path="render" type="function">
  Custom render function for advanced use cases
</ParamField>

<ParamField path="className" type="string">
  Additional CSS class name
</ParamField>

<ParamField path="attributes" type="object">
  Additional HTML attributes
</ParamField>

<ParamField path="children" type="React.ReactNode">
  Button content
</ParamField>

### Button.Group

<ParamField path="children" type="React.ReactNode" required>
  Node for inserting child Button components
</ParamField>

<ParamField path="className" type="string">
  Additional CSS class name
</ParamField>

<ParamField path="attributes" type="object">
  Additional HTML attributes
</ParamField>

### Button.Aligner

<ParamField path="side" type="'start' | 'end' | 'inline'">
  Side to align the button to
</ParamField>

<ParamField path="children" type="React.ReactElement" required>
  Button element to align
</ParamField>

<ParamField path="className" type="string">
  Additional CSS class name
</ParamField>

<ParamField path="attributes" type="object">
  Additional HTML attributes
</ParamField>